1. Uncomment actuator and web sections in pom.xml
2. Create a class that represents new endpoint that provides some operational information
    - use @Endpoint (at class level) and @ReadOperation (at method level) annotations
3. Verify with curl after running the app